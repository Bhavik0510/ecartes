<odoo>
    <data>
        <record model="ir.ui.view" id="view_invoice_asset_category_new">
            <field name="name">account.invoice.supplier.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='amount_residual']" position="after">
                    <!-- <field name="tcs_percentage" invisible="move_type = 'in_invoice' "/>
                    <field name="tcs_amounts" invisible="move_type = 'in_invoice' "/>
                    <field name="tds_percentage" invisible="move_type = 'out_invoice' "/>
                    <field name="tds_amt" invisible="move_type = 'out_invoice' "/>
                    <field name="net_amounts" invisible="move_type = 'out_invoice' "/> -->
                    <!-- <group> -->
                        <field name="tcs_percentage" invisible="move_type == 'in_invoice'"/>
                        <field name="tcs_amounts"   invisible="move_type == 'in_invoice'"/>
                        <field name="tds_percentage" invisible="move_type == 'out_invoice'"/>
                        <field name="tds_amt"        invisible="move_type == 'out_invoice'"/>
                        <field name="net_amounts"    invisible="move_type == 'out_invoice'"/>
                    <!-- </group> -->
                </xpath>
            </field>
        </record>
    </data>
</odoo>