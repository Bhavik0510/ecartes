<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Customer Invoice -->
    <record id="account_move_form_inherit_cash_rounding_invoice" model="ir.ui.view">
        <field name="name">account.move.form.inherit.cash.rounding.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_payment_term_id']" position="after">
                <field
                    name="invoice_cash_rounding_id"
                    options="{'no_create': True, 'no_open': True}"
                    readonly="state != 'draft'"
                    context="{'default_rounding_method': 'NEAREST'}"
                    invisible="move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')"
                />
            </xpath>
        </field>
    </record>
</odoo>